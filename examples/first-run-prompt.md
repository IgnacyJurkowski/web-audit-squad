# Example Claude Code prompt

```text
/web-audit-squad init and map this project. Use the scout output first. Then audit the landing page, pricing page, signup/auth flow, public booking page, and dashboard home in priority order. Keep it audit-only: update the audit Markdown files and backlog, but do not edit app code.
```
